# Environment
NODE_ENV=development
PORT=3001

# CORS
CORS_ORIGIN=http://localhost:5173

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wayshare
DB_USER=postgres
DB_PASSWORD=postgres

# JWT (for future use)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# AWS (for media storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-west-2
AWS_S3_BUCKET=wayshare-media

# Redis (for caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=